UVM(Unviersal Verification Methodology) 
  표준화된 검증 방법론
OOP (Object Oriented Programing) 
  객체지향 기반 표준 검증 방법론

test/sequence만 변경하여 다양한 시나리오 검증 가능 (재사용성)

run_phase(시간 흐름이 있는 phase)
  delay, event 사용 가능 → task로 선언
  function은 시간 흐름 사용 불가

clocking block
  race condition 방지용
  입력은 안정된 후 샘플링
  출력은 DUT와 충돌하지 않게 늦게 구동
  EX) input/output #1 : clock edge 기준 1 time unit 뒤에 샘플링/구동
      input/output #1step : 매우 작은 simulation step 기준으로 샘플링/구동

driver class 
  run_phase에서는 dut에서 값들이 실제로 구동하니까 보통 <=를 사용해서 넣어줌.
  driver에서는 item을 직접 만들지 않고, sequence에서 만든 item을 가져옴. (spi_seq_item item;  seq_item_port.get_next_item(item);)

monitor class
  run_phase에서는 구동 된 값을 복사하기만 하는것, 즉 읽기만 하니까 보통 =를 사용해서 복사함.
  monitor에서는 item을 직접 만들어야함. (item = spi_seq_item::type_id::create("item");)

monport
  interface에 다양한 신호들이 있는데, 아무 제한없이 모두 접근하게되면, 헷갈릴 수 있음.
  따라서 어떤 class에서는 몇몇의 신호들만 사용하게 임의로 정해줄수있음.(역할 고정)

uvm_analysis_port
- 값을 보내는 쪽
- write()로 데이터를 broadcast함
- 자체적으로 저장/비교는 안 함

uvm_analysis_imp
- 값을 받는 쪽
- write() 함수를 구현해서 데이터를 직접 받음
- 들어오자마자 바로 처리하는 방

uvm_tlm_analysis_fifo
- 값을 받는 쪽
- analysis port로 들어온 데이터를 FIFO처럼 저장함
- 나중에 get()으로 꺼내서 사용함
- expected/actual처럼 도착 시간이 다른 데이터를 맞춰 비교할 때 편함

component를 생성할 때
  ex)
    drv = uart_driver::type_id::create("drv", this);
    mon = uart_monitor::type_id::create("mon", this);
    sqr = uvm_sequencer#(uart_seq_item)::type_id::create("sqr", this);
item을 생성할 때
  ex) item = uart_seq_item::type_id::create("item");

new로 생성할 때
  constructor 내부의 기본 초기화
  factory override 필요 없는 일반 로컬 객체
  analysis port 생성 같은 것
  ex) ap = new("ap", this);
    








